{
  "name": "@moradbot/widget",
  "version": "0.1.0",
  "private": true,
  "description": "MoradBot Chat Widget - Customer-facing UI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "lint": "biome lint .",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@moradbot/shared": "workspace:*",
    "preact": "^10.24.3",
    "preact-render-to-string": "^6.5.11",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.2",
    "vite": "^6.0.5",
    "typescript": "^5.7.2"
  }
}
