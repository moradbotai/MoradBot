/**
 * MoradBot Chat Widget
 * Customer-facing UI
 */

import { render } from "preact";

function App() {
	return (
		<div>
			<h1>MoradBot Widget v0.1.0</h1>
		</div>
	);
}

render(<App />, document.getElementById("app")!);
